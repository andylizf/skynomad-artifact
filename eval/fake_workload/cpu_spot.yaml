name: fake-training-cpu

resources:
  cloud: aws
  instance_type: m6i.large  # Cheap CPU instance
  disk_size: 50
  use_spot: true

workdir: eval/fake_workload

setup: |
  echo "CBL-INSTANCE-PENDING"
  echo "[setup] Installing Python dependencies..."
  # No GPU setup needed

run: |
  echo "[run] Starting fake training workload..."
  python fake_train.py
